name: MCP Scan
description: Scan MCP configuration for unsafe tool declarations and embedded secrets.
inputs:
  path:
    description: Repository path to scan.
    required: false
    default: .
  allowed_tools:
    description: Comma-separated allowlist of MCP tool names.
    required: false
    default: ""
runs:
  using: docker
  image: Dockerfile
